using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using System.Web.Http;
using System.Web.Http.Results;
using Encode.API.Controllers;
using Encode.Models;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Encode.API.UnitTests
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public async Task TestMethod1()
        {
            //CustomersController customersController = new CustomersController();
            //IHttpActionResult result = await customersController.GetCustomers();

            //// Assert
            //var Result = result as OkNegotiatedContentResult<Customer>;


            //var contentResult = result as OkNegotiatedContentResult<Customer>;

            //Assert.IsNotNull(result);
            //Console.WriteLine(contentResult.Content);
            //Assert.IsNotNull(contentResult.Content);
        }
    }
}
